# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
build/
dist/

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# Secrets — never commit these
.env
.mcp.json
*.key

# Generated editions — kept local, never on git (preview images live in assets/)
/editions/
# Runtime output
/runs/
/issues/
# Per-page PNG previews are regenerable from the PDF/HTML
**/p-*.png
**/page-*.png
# Bulky prototype render artifacts (kept on disk, not in the repo)
**/out/

# Local-only agent instructions (never publish)
CLAUDE.local.md
CLAUDE.md

# Local-only notes (video build recipe, etc.) — never publish
*.local.md
# Session handoff notes — local only
handoff.md

# Local-only video build scripts (never publish)
.video-build/
